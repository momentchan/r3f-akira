/** Segment roles within one tendril tree. */
export const TENDRIL_ROLE = Object.freeze({
  /** Climb from the ground along the posed mesh to a wrap hitch. */
  GROUND_PATH: 'groundPath',
  /** Partial arc around a limb capsule. */
  WRAP: 'wrap',
});
